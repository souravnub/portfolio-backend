/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "know-sourav.netlify.app",
            },
        ],
    },
};

export default nextConfig;
